/**
 * Firebase Configuration
 *
 * This file contains Firebase project configuration settings.
 * It is committed with empty values and should be populated locally or via CI/CD.
 *
 * ## Local Development Setup
 * 1. Copy your Firebase project configuration from the Firebase Console
 * 2. Replace the empty strings below with your actual Firebase credentials
 * 3. This file is tracked in git but should contain your personal/team credentials locally
 *
 * ## CI/CD
 * GitHub Actions automatically populates this file with production credentials
 * from GitHub Secrets during the deployment process.
 */

/**
 * Firebase configuration template with empty values.
 * Replace these values with your Firebase project credentials for local development.
 */
export const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
